{% extends "base.html.twig" %}
{% block title %} MODIFICACIÓN TIPOS TRATAMIENTO {% endblock %}
{% block titulo %}
    <div id='dtitulo' class="container"> MODIFICACIÓN TIPOS DE TRATAMIENTO
                <a href="{{ path("add_cliente") }}"
                   class="btn btn-success">
                    <span class="glyphicon glyphicon-new-window"></span> Nuevo Cliente</a>
        {#        <a href="{{ path("query_cliente") }}"#}
        {#           class="btn btn-warning">#}
        {#            <span class="glyphicon glyphicon-refresh"></span> Volver</a>#}
        {#        <hr/>#}
    </div>
{% endblock %}

{% block contenido %}
    <script>
        $(document).ready(function () {

        });
    </script>
    <div class="container">
        {{ form_start (form,{'action':'','method':'POST'}) }}
        {{ form_errors(form) }}
        <div class="form-row"> {{ form_row(form.id) }} </div>
        <div class="form-row"> {{ form_row(form.descripcion) }} </div>
        <div class="form-row"> {{ form_row(form.importe) }} </div>
        <div class="form-row"> {{ form_row(form.tipoIva) }} </div>
        <div class="form-row"> {{ form_row(form.cuotaIva) }} </div>
        <div class="form-row"> {{ form_row(form.importeTotal) }} </div>
        <div class="form-row"> {{ form_row(form.Guardar) }} </div>
        {{ form_end (form) }}
    </div>

{% endblock %}
